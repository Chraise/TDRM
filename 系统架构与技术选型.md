# 2. 系统整体架构与技术选型

## 2.1 系统架构设计 (System Architecture)

### 2.1.1 架构模式

本项目采用 **MTV (Model-Template-View)** 架构模式，这是 Flask 框架的标准架构模式，与传统的 MVC 架构在概念上对应，但更符合 Flask 的设计理念。

**MTV 架构在本项目中的具体实现：**

1. **Model（模型层）**：位于 `app/models.py`，使用 SQLAlchemy ORM 定义数据库实体模型。模型层负责：
   - 定义数据表结构（如 `User`、`RepairOrder`、`DormBuilding`、`SparePart` 等）
   - 定义实体间的关系（一对多、多对多等）
   - 封装业务逻辑方法（如 `User.set_password()`、`User.check_password()`）
   - 提供数据验证和约束（如唯一性约束、检查约束）

2. **Template（模板层）**：位于 `app/templates/` 目录，使用 Jinja2 模板引擎渲染 HTML 页面。模板层负责：
   - 定义页面结构和布局（如 `base.html` 基础模板）
   - 实现页面复用（通过 `{% include %}` 和 `{% extends %}`）
   - 动态渲染数据（通过 `{{ variable }}` 和 `{% for %}` 等语法）
   - 分离不同角色的视图模板（`admin/`、`student/`、`worker/`、`auth/`）

3. **View（视图层）**：通过 Flask Blueprints 组织，位于 `app/admin/`、`app/auth/`、`app/student/`、`app/worker/` 等目录。视图层负责：
   - 处理 HTTP 请求和响应
   - 调用模型层进行数据查询和操作
   - 执行业务逻辑（如权限验证、状态流转）
   - 渲染模板并返回 HTML 响应

### 2.1.2 分层设计

#### 表现层 (Presentation Layer)

表现层采用前后端混合渲染架构，主要技术栈包括：

- **HTML5 + CSS3**：使用语义化 HTML 标签构建页面结构
- **Bootstrap 4**：采用 SB Admin 2 主题框架，提供响应式布局和组件库
- **JavaScript/jQuery**：处理前端交互逻辑，如表单验证、AJAX 请求
- **Chart.js**：用于数据可视化，在管理员仪表板中展示统计图表
- **FontAwesome**：提供图标库支持
- **Jinja2 模板引擎**：服务端模板渲染，实现动态内容生成

前端资源统一管理在 `app/static/` 目录下，包括：
- `css/`：样式文件（自定义样式和第三方框架样式）
- `js/`：JavaScript 脚本文件
- `img/`：图片资源
- `uploads/`：用户上传的报修图片

#### 业务逻辑层 (Business Layer)

业务逻辑层通过 Flask Blueprints 实现模块化组织，每个 Blueprint 对应一个功能模块：

- **`app/auth/`**：身份认证模块
  - 用户登录/登出
  - 密码重置（通过邮件）
  - 密码修改

- **`app/admin/`**：管理员模块
  - 仪表板数据统计
  - 用户管理（增删改查）
  - 宿舍楼管理
  - 报修单管理（派单、查看详情）
  - 配件库存管理

- **`app/student/`**：学生端模块
  - 报修单提交（支持图片上传）
  - 我的工单查询（支持状态筛选）
  - 工单详情查看
  - 工单评价

- **`app/worker/`**：维修工模块
  - 待办任务列表
  - 开始维修/完工填报
  - 配件消耗记录
  - 维修历史查询

每个 Blueprint 通过 `@bp.route()` 装饰器定义路由，并通过自定义装饰器（如 `@admin_required`、`@stu_required`、`@worker_required`）实现权限控制。

#### 数据持久层 (Data Persistence Layer)

数据持久层采用 SQLAlchemy ORM 框架，实现对象关系映射：

- **ORM 模型定义**：在 `app/models.py` 中使用 SQLAlchemy 2.0 现代语法（`Mapped`、`mapped_column`）定义数据模型
- **数据库连接管理**：通过 `Flask-SQLAlchemy` 扩展管理数据库连接池和会话
- **数据库迁移**：使用 `Flask-Migrate` 和 `Alembic` 进行数据库版本控制和迁移管理
- **数据库支持**：支持 SQLite（开发环境）和 MySQL（生产环境，通过 PyMySQL 驱动）
- **查询优化**：使用 `joinedload`、`selectinload` 等策略优化关联查询，避免 N+1 查询问题

数据库配置通过 `config.py` 统一管理，支持通过环境变量或 `.env` 文件配置数据库连接字符串。

## 2.2 技术栈详解 (Technology Stack)

| 技术组件 | 版本/说明 | 用途 |
|---------|----------|------|
| **后端框架** | | |
| Flask | 最新稳定版 | Python Web 框架，提供路由、请求处理、模板渲染等核心功能 |
| **数据库/ORM** | | |
| SQLAlchemy | 2.0+ | ORM 框架，提供对象关系映射和数据库抽象层 |
| Flask-SQLAlchemy | 最新版 | Flask 的 SQLAlchemy 扩展，简化数据库操作 |
| PyMySQL | 最新版 | MySQL 数据库驱动 |
| SQLite | 内置 | 开发环境默认数据库 |
| **前端技术** | | |
| Jinja2 | 3.x | 模板引擎，用于服务端渲染 HTML |
| Bootstrap 4 | SB Admin 2 主题 | 响应式 CSS 框架，提供 UI 组件和布局系统 |
| jQuery | 3.x | JavaScript 库，简化 DOM 操作和 AJAX 请求 |
| Chart.js | 最新版 | 数据可视化图表库，用于仪表板统计图表 |
| FontAwesome | 最新版 | 图标字体库 |
| **身份认证** | | |
| Flask-Login | 最新版 | 用户会话管理，提供登录状态维护和用户加载机制 |
| PyJWT | 2.10.1 | JWT 令牌生成和验证，用于密码重置功能 |
| **表单处理** | | |
| Flask-WTF | 1.2.2 | Flask 的 WTForms 集成，提供 CSRF 保护和表单验证 |
| WTForms | 3.2.1 | 表单定义和验证框架 |
| **数据迁移** | | |
| Flask-Migrate | 4.1.0 | 数据库迁移管理扩展 |
| Alembic | 1.17.2 | 数据库迁移工具，生成和执行迁移脚本 |
| **邮件服务** | | |
| Flask-Mail | 0.10.0 | 邮件发送扩展，用于密码重置邮件通知 |
| **时间处理** | | |
| zoneinfo | Python 3.9+ | 时区处理库，统一使用 Asia/Shanghai 时区 |
| python-dateutil | 2.9.0 | 日期时间解析和计算工具 |
| **配置管理** | | |
| python-dotenv | 1.2.1 | 环境变量管理，从 `.env` 文件加载配置 |
| **国际化** | | |
| Flask-Babel | 4.0.0 | 国际化支持（可选功能） |
| Flask-Moment | 1.0.6 | 时间格式化扩展 |
| **生产部署** | | |
| gunicorn | 23.0.0 | WSGI HTTP 服务器，用于生产环境部署 |
| **其他工具** | | |
| Werkzeug | 最新版 | WSGI 工具库，Flask 的底层依赖 |
| cryptography | 46.0.3 | 加密库，用于密码哈希和安全功能 |
| faker | 39.0.0 | 测试数据生成工具 |

## 2.3 项目代码结构 (Project Structure)

```
TDRM/
├── app/                          # 应用主目录
│   ├── __init__.py              # 应用工厂函数 create_app()
│   ├── models.py                # 数据库模型定义（User, RepairOrder, DormBuilding 等）
│   ├── decorators.py            # 权限控制装饰器（@admin_required, @stu_required, @worker_required）
│   ├── email.py                 # 邮件发送辅助模块
│   │
│   ├── admin/                   # 管理员模块 Blueprint
│   │   ├── __init__.py          # Blueprint 注册
│   │   ├── index.py             # 仪表板路由和统计 API
│   │   ├── user.py              # 用户管理路由
│   │   ├── building.py          # 宿舍楼管理路由
│   │   ├── order.py             # 报修单管理路由
│   │   ├── inventory.py         # 配件库存管理路由
│   │   └── forms.py             # 管理员模块表单定义
│   │
│   ├── auth/                    # 身份认证模块 Blueprint
│   │   ├── __init__.py          # Blueprint 注册
│   │   ├── routes.py            # 登录、登出、密码重置路由
│   │   └── forms.py             # 登录、密码重置表单定义
│   │
│   ├── student/                 # 学生端模块 Blueprint
│   │   ├── __init__.py          # Blueprint 注册
│   │   ├── routes.py            # 报修单提交、查询、评价路由
│   │   └── forms.py             # 报修单、评价表单定义
│   │
│   ├── worker/                  # 维修工模块 Blueprint
│   │   ├── __init__.py          # Blueprint 注册
│   │   ├── routes.py            # 任务列表、完工填报、历史查询路由
│   │   └── forms.py             # 维修记录表单定义
│   │
│   ├── templates/               # Jinja2 模板目录
│   │   ├── admin/               # 管理员端模板
│   │   │   ├── base.html        # 管理员端基础模板
│   │   │   ├── index.html       # 仪表板页面
│   │   │   ├── includes/        # 公共组件（侧边栏、顶部栏）
│   │   │   ├── user/            # 用户管理页面
│   │   │   ├── building/        # 宿舍楼管理页面
│   │   │   ├── order/           # 报修单管理页面
│   │   │   └── inventory/       # 配件库存管理页面
│   │   ├── student/             # 学生端模板
│   │   │   ├── base.html        # 学生端基础模板
│   │   │   ├── index.html       # 学生首页
│   │   │   ├── create_order.html # 报修单提交页面
│   │   │   ├── my_orders.html   # 我的工单列表
│   │   │   ├── order_detail.html # 工单详情
│   │   │   └── rate_order.html  # 工单评价页面
│   │   ├── worker/              # 维修工端模板
│   │   │   ├── base.html        # 维修工端基础模板
│   │   │   ├── index.html       # 维修工仪表板
│   │   │   ├── task_list.html   # 待办任务列表
│   │   │   ├── finish_form.html # 完工填报表单
│   │   │   └── history.html     # 维修历史
│   │   ├── auth/                # 认证相关模板
│   │   │   ├── login.html       # 登录页面
│   │   │   ├── reset_password_request.html # 密码重置请求
│   │   │   ├── reset_password.html # 密码重置表单
│   │   │   └── change_password.html # 修改密码
│   │   ├── email/               # 邮件模板
│   │   │   ├── reset_password.html # 密码重置邮件（HTML）
│   │   │   └── reset_password.txt  # 密码重置邮件（文本）
│   │   └── common/              # 公共模板组件
│   │       └── _macros.html     # Jinja2 宏定义
│   │
│   └── static/                  # 静态资源目录
│       ├── css/                 # 样式文件
│       │   ├── sb-admin-2.min.css # Bootstrap 主题样式
│       │   └── admin-custom.css   # 自定义样式
│       ├── js/                  # JavaScript 文件
│       │   ├── sb-admin-2.min.js  # 主题脚本
│       │   ├── utils.js          # 工具函数（时区转换等）
│       │   └── demo/              # 图表示例脚本
│       ├── img/                 # 图片资源
│       ├── uploads/             # 用户上传的报修图片
│       └── vendor/              # 第三方库
│           ├── bootstrap/       # Bootstrap 框架
│           ├── jquery/          # jQuery 库
│           ├── chart.js/        # Chart.js 图表库
│           ├── datatables/      # DataTables 表格插件
│           └── fontawesome-free/ # FontAwesome 图标库
│
├── migrations/                  # 数据库迁移目录
│   ├── alembic.ini              # Alembic 配置文件
│   ├── env.py                   # 迁移环境配置
│   └── versions/                # 迁移脚本版本目录
│       └── *.py                 # 迁移脚本文件
│
├── config.py                    # 应用配置文件（数据库、邮件等配置）
├── TDRM.py                      # 应用入口文件
├── requirements.txt             # Python 依赖包列表
├── seed.py                      # 数据库种子数据脚本
├── Dockerfile                   # Docker 镜像构建文件
├── docker-compose.yml           # Docker Compose 编排文件
└── README.md                    # 项目说明文档
```

### 核心目录说明

- **`app/__init__.py`**：应用工厂函数 `create_app()`，采用 Application Factory Pattern，负责初始化 Flask 应用、注册扩展和 Blueprints
- **`app/models.py`**：数据库模型定义，包含 6 个核心实体模型（User、RepairOrder、DormBuilding、SparePart、MaintenanceRecord、PartUsageDetail）及其关系定义
- **`app/decorators.py`**：权限控制装饰器，实现基于角色的访问控制（RBAC）
- **`app/templates/`**：Jinja2 模板目录，按功能模块组织，使用模板继承和包含实现代码复用
- **`app/static/`**：静态资源目录，包含 CSS、JavaScript、图片和第三方库文件
- **`migrations/`**：数据库迁移目录，使用 Alembic 管理数据库版本变更
- **`config.py`**：应用配置类，支持从环境变量加载配置，实现开发/生产环境配置分离

## 2.4 核心设计规范

### 2.4.1 应用工厂模式 (Application Factory Pattern)

项目采用 **Application Factory Pattern** 创建 Flask 应用，实现在 `app/__init__.py` 中的 `create_app()` 函数。这种模式的优势包括：

- **测试友好**：可以为不同测试场景创建不同配置的应用实例
- **多实例支持**：可以同时运行多个应用实例（如开发服务器和生产服务器）
- **延迟初始化**：扩展（如数据库、登录管理器）在应用创建时初始化，而非全局初始化
- **配置灵活**：通过 `config_class` 参数支持不同环境配置

```python
def create_app(config_class=Config):
    app = Flask(__name__)
    app.config.from_object(config_class)
    
    # 初始化扩展
    db.init_app(app)
    migrate.init_app(app, db)
    login_manager.init_app(app)
    mail.init_app(app)
    moment.init_app(app)
    
    # 注册 Blueprints
    app.register_blueprint(auth_bp, url_prefix='/auth')
    app.register_blueprint(admin_bp, url_prefix='/admin')
    app.register_blueprint(student_bp, url_prefix='/student')
    app.register_blueprint(worker_bp, url_prefix='/worker')
    
    return app
```

### 2.4.2 权限控制机制

项目实现了基于角色的访问控制（RBAC），通过装饰器模式实现权限验证：

1. **角色枚举定义**：在 `app/models.py` 中定义 `UserRole` 枚举（`STUDENT`、`WORKER`、`ADMIN`）

2. **装饰器实现**：在 `app/decorators.py` 中实现通用装饰器 `role_required()`，并导出三个具体装饰器：
   - `@admin_required`：仅管理员可访问
   - `@stu_required`：仅学生可访问
   - `@worker_required`：仅维修工可访问

3. **使用方式**：在路由函数上使用装饰器，未授权用户访问时自动重定向到登录页面并显示提示信息

```python
@bp.route('/index')
@admin_required
def index():
    # 仅管理员可访问的路由逻辑
    pass
```

4. **Flask-Login 集成**：结合 `Flask-Login` 的 `@login_required` 装饰器，实现登录状态检查

### 2.4.3 时区处理策略

项目采用统一的时区处理策略，确保时间数据的一致性和准确性：

1. **存储策略**：所有时间数据以 **UTC（协调世界时）** 格式存储到数据库，避免时区转换问题

2. **显示策略**：前端显示时统一转换为 **Asia/Shanghai（东八区）** 时区，使用 `zoneinfo.ZoneInfo("Asia/Shanghai")` 进行转换

3. **实现方式**：
   - 后端：在 `app/admin/index.py`、`app/worker/routes.py`、`app/student/routes.py` 中使用 `ZoneInfo("Asia/Shanghai")` 进行时区转换
   - 前端：在 `app/static/js/utils.js` 中提供 JavaScript 函数，自动将 UTC 时间转换为本地时区显示

4. **时间计算**：涉及日期范围查询时（如"今天"、"本周"、"本月"），先计算本地时区的起止时间，再转换为 UTC 进行数据库查询

```python
SHANGHAI_TZ = ZoneInfo("Asia/Shanghai")

def local_today_range():
    """计算本地时区今天的起止时间（UTC）"""
    now_local = datetime.now(LOCAL_TZ)
    start_local = now_local.replace(hour=0, minute=0, second=0, microsecond=0)
    end_local = start_local + timedelta(days=1)
    return start_local.astimezone(timezone.utc), end_local.astimezone(timezone.utc)
```

### 2.4.4 其他设计规范

1. **表单验证**：使用 `Flask-WTF` 和 `WTForms` 进行服务端表单验证，防止 CSRF 攻击，确保数据安全

2. **文件上传**：报修图片上传功能实现文件类型验证、安全文件名处理、唯一文件名生成（UUID），并支持异常回滚时删除已上传文件

3. **密码安全**：使用 `werkzeug.security.generate_password_hash()` 和 `check_password_hash()` 进行密码哈希存储，不存储明文密码

4. **邮件异步发送**：密码重置邮件通过 `threading.Thread` 异步发送，避免阻塞请求处理

5. **数据库查询优化**：使用 SQLAlchemy 的 `joinedload`、`selectinload` 等加载策略，避免 N+1 查询问题，提高查询效率

6. **错误处理**：使用 Flask 的 `abort()` 函数和异常处理机制，提供友好的错误提示页面

7. **代码组织**：遵循单一职责原则，每个 Blueprint 负责一个功能模块，模型、视图、表单分离，提高代码可维护性

