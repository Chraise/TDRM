# 3. 系统功能设计

本章节详细描述《高校宿舍报修管理系统》各功能模块的设计与实现逻辑，按照 Flask Blueprint 的模块划分（Auth、Student、Worker、Admin）进行组织，并结合数据库实体模型和业务状态流转进行说明。

## 3.1 认证与授权模块 (Auth Module)

认证与授权模块负责用户身份验证、会话管理和密码安全功能，是整个系统的安全入口。

### 3.1.1 用户登录/登出

#### 功能概述
用户通过账号（学号/工号）和密码登录系统，系统根据用户角色（`UserRole` 枚举：`STUDENT`、`WORKER`、`ADMIN`）自动重定向到对应的功能模块首页。

#### 技术实现

**会话管理机制**：
- 使用 `Flask-Login` 扩展管理用户会话状态
- 通过 `LoginManager` 配置登录视图和提示信息
- 用户模型 `User` 继承 `UserMixin`，实现 `get_id()` 方法返回用户ID（字符串格式）
- 登录状态通过 `current_user.is_authenticated` 属性判断

**登录流程**：
1. 用户提交登录表单（`LoginForm`），包含账号、密码和"记住我"选项
2. 后端验证表单数据：
   - 账号格式校验：纯数字，长度 4-20 位
   - 密码必填校验
3. 数据库查询：根据账号查询 `User` 表，使用 `check_password()` 方法验证密码哈希
4. 登录成功处理：
   - 调用 `login_user(user, remember=form.remember_me.data)` 创建会话
   - 根据用户角色调用 `get_redirect_url_by_role()` 函数重定向：
     - `ADMIN` → `/admin/index`（管理员仪表板）
     - `STUDENT` → `/student/index`（学生首页）
     - `WORKER` → `/worker/index`（维修工工作台）
5. 登录失败：显示错误提示，保持在登录页面

**登出流程**：
- 调用 `logout_user()` 清除会话
- 重定向到登录页面

**安全特性**：
- 密码使用 `werkzeug.security.generate_password_hash()` 进行哈希存储，不存储明文
- 支持"记住我"功能，通过 Flask-Login 的会话持久化实现
- 已登录用户访问登录页面时自动重定向到对应角色首页，避免重复登录

### 3.1.2 密码重置

#### 功能概述
用户忘记密码时，可通过邮箱接收重置链接，使用 JWT Token 验证身份后重置密码。

#### 技术实现

**密码重置流程**：

1. **请求重置密码**（`/auth/reset_password_request`）：
   - 用户输入注册邮箱
   - 后端查询 `User` 表验证邮箱是否存在
   - 如果邮箱存在，调用 `User.get_reset_password_token()` 生成 JWT Token
   - Token 包含用户ID和过期时间（默认 600 秒）
   - 使用 `Flask-Mail` 异步发送重置邮件（通过 `threading.Thread` 实现，避免阻塞请求）

2. **邮件内容**：
   - HTML 模板：`templates/email/reset_password.html`
   - 文本模板：`templates/email/reset_password.txt`
   - 包含重置链接：`/auth/reset_password/<token>`

3. **重置密码**（`/auth/reset_password/<token>`）：
   - 调用 `User.verify_reset_password_token(token)` 验证 Token：
     - 使用 `PyJWT` 解码 Token
     - 验证签名和过期时间
     - 返回对应的 `User` 对象或 `None`
   - 如果 Token 无效或过期，显示错误提示
   - 如果 Token 有效，显示密码重置表单（`ResetPasswordForm`）
   - 用户提交新密码后：
     - 验证两次密码输入一致（使用 `EqualTo` 验证器）
     - 调用 `user.set_password(new_password)` 更新密码哈希
     - 提交数据库事务
     - 重定向到登录页面

**安全特性**：
- JWT Token 使用应用密钥（`SECRET_KEY`）签名，防止篡改
- Token 设置过期时间，防止长期有效导致的安全风险
- 邮件异步发送，不阻塞用户请求
- 密码重置后自动清除 Token，防止重复使用

### 3.1.3 密码修改

#### 功能概述
已登录用户可在个人设置中修改密码，需要验证旧密码。

#### 技术实现

**密码修改流程**（`/auth/change_password`）：
1. 用户必须已登录（使用 `@login_required` 装饰器）
2. 提交修改密码表单（`ChangePasswordForm`），包含：
   - 旧密码（必填）
   - 新密码（必填）
   - 确认密码（必填，需与新密码一致）
3. 后端验证：
   - 使用 `current_user.check_password(form.old_password.data)` 验证旧密码
   - 检查新密码是否与旧密码相同（防止无意义修改）
4. 密码更新：
   - 调用 `current_user.set_password(form.password.data)` 更新密码
   - 提交数据库事务
   - 登出当前会话（`logout_user()`），要求用户使用新密码重新登录
   - 显示成功提示并重定向到登录页面

## 3.2 学生端功能模块 (Student Module)

学生端模块为学生用户提供报修单提交、查询、评价等功能。

### 3.2.1 提交报修单

#### 功能概述
学生可以提交报修单，包括故障描述、位置信息和图片上传，系统自动关联学生所属宿舍楼和用户信息。

#### 输入数据
- **故障位置**（`repair_location`）：房间号或具体位置，字符串，最大长度 10 字符
- **报修主题**（`title`）：简要描述故障类型，字符串，最大长度 100 字符
- **详细描述**（`description`）：故障详细情况，文本字段，最大长度 1000 字符（可选）
- **故障图片**（`image_urls`）：支持多张图片上传，格式限制为 jpg/jpeg/png/gif（可选）

#### 业务逻辑

**数据关联与初始化**：
1. 从当前登录用户（`current_user`）获取：
   - `submitter_id`：报修人ID（`current_user.user_id`）
   - `repair_building_id`：故障所在楼ID（`current_user.building.building_id`）
   - `repair_location`：默认填充用户房间号（`current_user.room_no`）
2. 权限校验：检查用户是否关联宿舍楼，未关联则提示错误并禁止提交

**图片处理逻辑**：
1. 文件验证：
   - 检查文件扩展名是否在允许列表（`ALLOWED_EXTENSIONS`）中
   - 使用 `secure_filename()` 处理文件名，防止路径遍历攻击
2. 文件存储：
   - 生成唯一文件名：`{uuid4().hex}_{原始文件名}`
   - 保存到 `app/static/uploads/` 目录
   - 数据库存储：将相对路径（如 `uploads/xxx.jpg`）以逗号分隔的字符串形式存入 `RepairOrder.image_urls` 字段
3. 异常处理：
   - 如果数据库提交失败，自动回滚并删除已上传的文件
   - 使用 `try-except` 捕获异常，确保文件系统与数据库的一致性

**工单创建**：
1. 创建 `RepairOrder` 实例：
   - `status` 初始化为 `OrderStatus.PENDING`（待处理状态）
   - `submit_time` 自动设置为当前 UTC 时间
   - `rating` 和 `feedback` 初始化为 `None`
2. 数据库操作：
   - 使用 `db.session.add()` 添加到会话
   - 调用 `db.session.commit()` 提交事务
3. 成功反馈：显示成功提示，重定向到学生首页

#### 状态流转
- **初始状态**：`PENDING`（待处理）
- 后续状态由管理员派单和维修工操作触发，详见 3.3 和 3.4 节

### 3.2.2 我的工单查询

#### 功能概述
学生可以查看自己提交的所有报修单，支持按状态筛选和智能排序。

#### 业务逻辑

**权限控制**：
- 使用 `@stu_required` 装饰器确保仅学生角色可访问
- 查询条件：`RepairOrder.submitter_id == current_user.user_id`，确保学生只能查看自己提交的工单

**状态筛选**：
- 支持查询参数 `status`，可选值：`PENDING`、`ASSIGNED`、`IN_PROGRESS`、`COMPLETED`、`CANCELLED`
- 如果指定状态，使用 `where(RepairOrder.status == OrderStatus[status_param])` 过滤

**智能排序**：
- 使用 SQLAlchemy `case` 表达式定义状态权重：
  - `PENDING` = 1（最高优先级）
  - `ASSIGNED` = 2
  - `IN_PROGRESS` = 3
  - `COMPLETED` = 4
  - `CANCELLED` = 5
- 排序规则：先按状态权重升序，再按提交时间降序
- 确保待处理工单始终显示在最前面

**数据加载优化**：
- 使用 `selectinload()` 预加载关联数据：
  - `RepairOrder.maintenance_records`（维修记录）
  - `MaintenanceRecord.worker`（维修工信息）
- 避免 N+1 查询问题，提高查询效率

### 3.2.3 工单详情查看

#### 功能概述
学生可以查看报修单的详细信息，包括故障描述、维修进度、维修记录等。

#### 业务逻辑

**权限校验**：
1. 查询工单：根据 `order_id` 查询 `RepairOrder` 表
2. 权限验证：检查 `order.submitter_id == current_user.user_id`，确保只能查看自己的工单
3. 如果权限不足，使用 `abort(403)` 返回 403 错误

**数据加载**：
- 使用 `joinedload()` 和 `selectinload()` 预加载：
  - `RepairOrder.building`（宿舍楼信息）
  - `RepairOrder.assigned_workers`（指派的维修工列表）
  - `RepairOrder.maintenance_records`（维修记录列表）
  - `MaintenanceRecord.worker`（每个维修记录的维修工信息）

**维修记录排序**：
- 对 `order.maintenance_records` 按时间排序：
  - 优先使用 `end_time`，如果为 `None` 则使用 `start_time`
  - 降序排列，最新记录在前

**评价入口控制**：
- 计算 `can_rate` 标志：
  - 工单状态必须为 `COMPLETED`
  - 工单的 `rating` 字段必须为 `None`（未评价）
- 只有满足条件时才显示评价入口

### 3.2.4 工单评价

#### 功能概述
学生对已完成的工单进行服务评价，包括评分（1-5星）和文字反馈。

#### 业务逻辑

**权限与状态校验**：
1. 权限验证：确保工单属于当前用户（`order.submitter_id == current_user.user_id`）
2. 状态校验：工单状态必须为 `COMPLETED`
3. 防重复评价：检查 `order.rating is None`，如果已评价则提示并重定向

**评价提交**：
1. 表单验证：`RateOrderForm` 包含：
   - `rating`：单选评分（1-5星），必填
   - `feedback`：文字反馈，可选，最大长度 255 字符
2. 数据更新：
   - `order.rating = form.rating.data`
   - `order.feedback = form.feedback.data`
   - 提交数据库事务
3. 成功反馈：显示感谢提示，重定向到工单详情页

**数据持久化**：
- 评价数据直接更新到 `RepairOrder` 表的 `rating` 和 `feedback` 字段
- 评价后不可修改，确保评价的真实性

### 3.2.5 工单取消

#### 功能概述
学生可以取消处于"待处理"状态的工单。

#### 业务逻辑

**取消条件**：
1. 权限验证：确保工单属于当前用户
2. 状态限制：仅允许取消 `PENDING` 状态的工单
3. 如果工单已派单或处理中，提示"工单已处理或已完成，无法取消"

**取消操作**：
- 将工单状态更新为 `OrderStatus.CANCELLED`
- 提交数据库事务
- 显示成功提示，重定向到工单详情页

## 3.3 维修工端功能模块 (Worker Module)

维修工端模块为维修人员提供任务管理、维修执行和耗材登记等功能。

### 3.3.1 任务工作台

#### 功能概述
维修工工作台显示待办任务统计、今日/本周/本月完成数量，以及紧急任务列表。

#### 业务逻辑

**任务查询机制**：
- 使用 SQLAlchemy 关系查询：`RepairOrder.assigned_workers.any(User.user_id == current_user.user_id)`
- 该查询通过 `order_assign` 中间表（多对多关系表）关联：
  - `order_assign.user_id` = 当前维修工ID
  - `order_assign.order_id` = 工单ID

**统计数据计算**：

1. **待处理任务数**（`pending_count`）：
   - 查询条件：工单状态为 `ASSIGNED` 或 `IN_PROGRESS`
   - 使用 `func.count()` 聚合统计

2. **今日完成数**（`today_completed`）：
   - 时区处理：使用 `local_today_range()` 函数计算本地时区（Asia/Shanghai）今天的起止时间，转换为 UTC
   - 查询条件：工单状态为 `COMPLETED`，且 `finish_time` 在今日时间范围内

3. **本周完成数**（`week_completed`）：
   - 使用 `local_week_range()` 计算本周起止时间（周一 00:00 至下周一 00:00）

4. **本月完成数**（`month_completed`）：
   - 使用 `local_month_range()` 计算本月起止时间（1日 00:00 至下月1日 00:00）

**紧急任务列表**：
- 查询条件：工单状态为 `ASSIGNED` 或 `IN_PROGRESS`
- 排序规则：按提交时间升序（最早提交的优先）
- 限制数量：最多显示 10 条
- 数据预加载：使用 `joinedload()` 和 `selectinload()` 加载关联数据

### 3.3.2 我的待办任务列表

#### 功能概述
维修工查看分配给自己的所有待办任务，支持按状态分类显示。

#### 业务逻辑

**任务筛选**：
- 查询条件：工单状态为 `ASSIGNED` 或 `IN_PROGRESS`
- 使用关系查询确保只显示分配给当前维修工的工单

**智能排序**：
- 状态权重：`ASSIGNED` = 1（优先），`IN_PROGRESS` = 2
- 排序规则：先按状态权重升序，再按提交时间降序
- 确保新派单的任务优先显示

**数据展示**：
- 显示工单基本信息：标题、提交时间、故障位置、宿舍楼
- 显示其他协作维修工（如果多人协作）
- 显示报修人信息
- 标记已完成任务：查询 `MaintenanceRecord` 表，标记当前维修工已提交维修记录的工单

### 3.3.3 开始维修

#### 功能概述
维修工确认接单，将工单状态从"已派单"（`ASSIGNED`）流转为"处理中"（`IN_PROGRESS`）。

#### 业务逻辑

**权限与状态校验**：
1. 权限验证：确保当前维修工在工单的 `assigned_workers` 列表中
2. 状态检查：工单状态必须为 `ASSIGNED`
3. 如果状态已经是 `IN_PROGRESS`，提示"工单已处于处理中状态"

**状态更新**：
- 将工单状态更新为 `OrderStatus.IN_PROGRESS`
- 提交数据库事务
- 显示成功提示，重定向到任务列表

**业务意义**：
- 状态流转：`ASSIGNED` → `IN_PROGRESS`
- 表示维修工已开始处理该工单，工单进入执行阶段

### 3.3.4 完工填报与耗材登记（核心功能）

#### 功能概述
维修工完成维修后，填写维修记录，包括维修结果、工时和消耗的配件，系统自动扣减库存并计算成本。

#### 输入数据
- **开始时间**（`start_time`）：维修开始时间，本地时区（Asia/Shanghai）
- **结束时间**（`end_time`）：维修结束时间，本地时区
- **维修结果**（`result_desc`）：故障原因分析和维修结果描述，文本字段
- **配件消耗**（`part_ids[]`、`quantities[]`）：配件ID数组和对应数量数组（可选）

#### 业务逻辑

**权限与重复提交校验**：
1. 权限验证：确保当前维修工在工单的 `assigned_workers` 列表中
2. 重复提交检查：查询 `MaintenanceRecord` 表，检查是否已存在当前维修工对该工单的维修记录
3. 如果已提交，提示"您已提交过该工单的维修记录，无需重复提交"

**时间处理**：
- 前端提交的本地时间（无时区）通过 `naive_to_utc()` 函数转换为 UTC 时间
- 计算工时：`duration_hours = (end_time - start_time).total_seconds() / 3600`
- 计算人工成本：`labor_cost = duration_hours * HOURLY_RATE`（时薪 50 元/小时）

**事务处理流程**（核心逻辑）：

1. **开启数据库事务**（隐式，通过 SQLAlchemy 会话管理）

2. **创建维修记录**：
   ```python
   record = MaintenanceRecord(
       order_id=order.order_id,
       worker_id=current_user.user_id,
       start_time=start_utc,
       end_time=end_utc,
       result_desc=form.result_desc.data,
       labor_cost=labor_cost
   )
   db.session.add(record)
   db.session.flush()  # 获取 record_id，用于后续关联
   ```

3. **处理配件消耗**（如果存在）：
   - 遍历前端提交的配件ID和数量数组
   - 对每个配件执行以下步骤（在同一事务中）：
     a. **库存校验**：
        - 查询 `SparePart` 表获取配件信息
        - 检查 `part.current_stock >= qty`，如果不足则抛出 `ValueError` 异常
     b. **扣减库存**：
        - `part.current_stock -= qty`
        - 直接更新对象属性，SQLAlchemy 会自动生成 UPDATE 语句
     c. **创建消耗明细**：
        - 创建 `PartUsageDetail` 记录，关联 `record_id` 和 `part_id`
        - `db.session.add(detail)`

4. **工单完成判断**：
   - 统计该工单的所有维修记录数：`completed_count = count(MaintenanceRecord where order_id = order.order_id)`
   - 获取工单指派的维修工总数：`total_workers = len(order.assigned_workers)`
   - 如果 `completed_count >= total_workers`：
     - 更新工单状态为 `OrderStatus.COMPLETED`
     - 设置 `order.finish_time = datetime.now(timezone.utc)`
     - 提示"所有维修人员均已完工，工单归档"
   - 否则提示当前进度（如"当前进度 2/3，等待其他同事完工"）

5. **提交事务**：
   - `db.session.commit()`：如果所有操作成功，提交事务
   - 如果任何步骤失败（如库存不足、数据库错误），自动回滚（`db.session.rollback()`）

**异常处理**：
- `ValueError`：库存不足或配件不存在，回滚事务并显示具体错误信息
- 其他异常：记录日志，回滚事务，显示通用错误提示

**数据一致性保证**：
- 所有操作在同一事务中执行，确保原子性
- 库存扣减与消耗明细创建同步，避免数据不一致
- 如果工单完成判断失败，不影响维修记录的创建

### 3.3.5 维修历史查询

#### 功能概述
维修工查看自己参与的所有已完成工单的历史记录。

#### 业务逻辑

**查询条件**：
- 工单状态为 `COMPLETED`
- 当前维修工在工单的 `assigned_workers` 列表中
- 按完成时间降序排列

**数据展示**：
- 显示工单基本信息、完成时间、评分和反馈
- 显示协作维修工信息
- 显示报修人信息

## 3.4 管理员端功能模块 (Admin Module)

管理员端模块提供系统管理、工单调度、库存管理等核心管理功能。

### 3.4.1 数据仪表板

#### 功能概述
管理员仪表板展示系统关键指标和统计数据，包括待处理工单数、库存预警、月度完成数等。

#### 关键指标统计

**实时指标**（`/admin/index`）：

1. **待处理工单数**（`pending_count`）：
   - 查询条件：`RepairOrder.status == OrderStatus.PENDING`
   - 使用 `func.count()` 聚合统计
   - 反映当前系统积压情况

2. **低库存预警数**（`low_stock_count`）：
   - 查询条件：`SparePart.current_stock < SparePart.warning_line`
   - 反映需要补货的配件数量

3. **本月完成工单数**（`finished_monthly_count`）：
   - 时区处理：计算本地时区（Asia/Shanghai）本月的起止时间，转换为 UTC
   - 查询条件：`RepairOrder.status == OrderStatus.COMPLETED` 且 `finish_time >= month_start_utc`
   - 反映本月工作效率

4. **系统用户总数**（`total_users_count`）：
   - 统计 `User` 表总记录数

**统计图表数据**（`/admin/api/dashboard/stats`）：

1. **趋势分析**（`trend`）：
   - 时间范围：默认最近 7 天，支持自定义日期范围
   - 数据项：
     - `new_orders`：每日新增工单数（按 `submit_time` 分组）
     - `completed_orders`：每日完成工单数（按 `finish_time` 分组）
   - 使用 `func.date()` 函数提取日期部分进行分组
   - 填充缺失日期：确保日期范围内所有日期都有数据点（缺失日期填充为 0）

2. **状态分布**（`status_distribution`）：
   - 按工单状态分组统计数量
   - 状态映射：`PENDING` → "待处理"，`ASSIGNED` → "已派单"，`IN_PROGRESS` → "处理中"，`COMPLETED` → "已完成"，`CANCELLED` → "已取消"
   - 用于饼图展示

3. **Top 5 楼宇**（`top_buildings`）：
   - 按宿舍楼分组统计工单数，降序排列，取前 5 名
   - 使用 `JOIN` 关联 `DormBuilding` 和 `RepairOrder` 表
   - 用于柱状图展示

4. **配件消耗趋势**（`parts_consumption`）：
   - 时间范围：最近 6 个月
   - 数据项：Top 5 配件（基于最近 6 个月总使用量）的月度消耗趋势
   - 查询逻辑：
     a. 先查询最近 6 个月总使用量最多的 5 个配件
     b. 再查询这 5 个配件每个月的消耗量（按 `RepairOrder.finish_time` 的月份分组）
     c. 使用 `func.date_format(RepairOrder.finish_time, '%Y-%m')` 提取年月
   - 用于堆叠柱状图展示，每个配件使用不同颜色

**前端展示**：
- 使用 Chart.js 库渲染图表
- 支持日期范围筛选，通过 AJAX 请求更新图表数据

### 3.4.2 工单调度 (Dispatch)

#### 功能概述
管理员查看待处理工单，并将工单指派给一名或多名维修工，系统自动更新工单状态。

#### 业务逻辑

**工单列表查询**（`/admin/orders`）：

1. **筛选功能**：
   - 默认显示"活跃工单"（状态为 `PENDING`、`ASSIGNED` 或 `IN_PROGRESS`）
   - 支持切换到"历史工单"（所有状态）
   - 支持关键词搜索：工单ID、标题、报修人姓名、宿舍楼名称

2. **智能排序**：
   - 活跃工单：先按状态权重升序，再按提交时间升序（FIFO，最早提交的优先处理）
   - 历史工单：先按状态权重升序，再按提交时间降序（最新显示）

3. **数据预加载**：
   - 使用 `joinedload()` 和 `selectinload()` 预加载：
     - `RepairOrder.submitter`（报修人）
     - `RepairOrder.building`（宿舍楼）
     - `RepairOrder.assigned_workers`（已指派的维修工列表）

**派单操作**（`/admin/orders/dispatch/<order_id>`）：

1. **权限与工单校验**：
   - 使用 `@admin_required` 装饰器确保仅管理员可访问
   - 验证工单是否存在

2. **维修工选择**：
   - 表单字段：`DispatchForm.worker_ids`（多选字段）
   - 选项来源：查询 `User` 表，筛选 `role == 'Worker'` 且 `status == 1`（在职）的维修工
   - 表单验证：至少选择一位维修工

3. **派单事务处理**：
   ```python
   # 查询选中的维修工
   workers = db.session.execute(
       select(User).where(User.user_id.in_(worker_ids))
   ).scalars().all()
   
   # 更新工单的 assigned_workers 关系
   order.assigned_workers = workers  # SQLAlchemy 自动处理中间表更新
   
   # 如果工单状态为 PENDING，自动更新为 ASSIGNED
   if order.status == OrderStatus.PENDING:
       order.status = OrderStatus.ASSIGNED
   
   db.session.commit()
   ```

4. **中间表更新机制**：
   - SQLAlchemy 通过 `order_assign` 中间表（多对多关系表）管理工单与维修工的关联
   - 当设置 `order.assigned_workers = workers` 时，SQLAlchemy 自动：
     - 删除旧的关联记录（如果存在）
     - 插入新的关联记录
   - 中间表结构：`(user_id, order_id)` 复合主键

5. **状态流转**：
   - `PENDING` → `ASSIGNED`（如果工单原状态为待处理）
   - 如果工单已经是 `ASSIGNED` 或 `IN_PROGRESS`，仅更新指派关系，不改变状态

6. **成功反馈**：
   - 显示成功提示，包含工单号和指派的维修工姓名
   - 重定向到工单列表

**推荐维修工功能**（`/admin/api/recommend-worker`）：

- 算法：统计每个维修工的活跃工单数（状态为 `ASSIGNED` 或 `IN_PROGRESS`）
- 推荐逻辑：选择活跃工单数最少的维修工（工作负载最轻）
- 查询实现：
  - 使用 `LEFT JOIN` 关联 `order_assign` 和 `RepairOrder` 表
  - 按 `user_id` 分组，统计工单数
  - 按工单数升序排列，取第一条记录
- 返回 JSON 格式数据，供前端 AJAX 调用

### 3.4.3 工单详情查看

#### 功能概述
管理员查看工单的完整信息，包括报修信息、维修进度、维修记录等。

#### 业务逻辑

**数据加载**：
- 使用 `joinedload()` 和 `selectinload()` 预加载所有关联数据：
  - `RepairOrder.building`（故障所在楼）
  - `RepairOrder.submitter`（报修人）及其关联的 `building`（报修人所属楼）
  - `RepairOrder.assigned_workers`（指派的维修工列表）
  - `RepairOrder.maintenance_records`（维修记录列表）
  - `MaintenanceRecord.worker`（每个维修记录的维修工信息）

**维修记录排序**：
- 按时间降序排列（最新记录在前），优先使用 `end_time`，如果为 `None` 则使用 `start_time`

### 3.4.4 库存管理

#### 功能概述
管理员管理配件库存，包括新增、编辑、删除配件，以及快速补货和低库存预警。

#### 业务逻辑

**库存列表**（`/admin/inventory`）：

1. **低库存预警优先显示**：
   - 查询所有配件
   - 排序规则：低库存配件（`current_stock < warning_line`）优先显示，然后按配件ID降序
   - 使用 SQLAlchemy 表达式：`order_by((SparePart.current_stock < SparePart.warning_line).desc(), SparePart.part_id.desc())`

2. **数据展示**：
   - 显示配件名称、规格、当前库存、单位、单价、预警阈值
   - 低库存配件使用醒目标识（前端样式）

**新增配件**（`/admin/inventory/add`）：

1. **表单字段**（`SparePartForm`）：
   - `part_name`：配件名称（必填，最大长度 100 字符）
   - `spec`：规格型号（可选，最大长度 50 字符）
   - `unit`：单位（可选，如"个"、"米"、"升"）
   - `price`：单价（必填，非负数，保留 2 位小数）
   - `warning_line`：库存预警阈值（必填，非负数，默认 10）
   - `current_stock`：初始库存（可选，非负数，默认 0）

2. **数据验证**：
   - 服务端验证：使用 WTForms 验证器
   - 数据库约束：`part_name` 和 `spec` 的组合唯一性（通过 `UniqueConstraint` 实现）

3. **创建逻辑**：
   - 创建 `SparePart` 实例
   - 添加到数据库会话并提交
   - 显示成功提示，重定向到库存列表

**编辑配件**（`/admin/inventory/edit/<part_id>`）：

1. **数据加载**：
   - 根据 `part_id` 查询配件
   - 使用 `form = SparePartForm(obj=part)` 将数据库对象绑定到表单

2. **更新逻辑**：
   - 更新配件所有字段（除主键外）
   - 提交数据库事务
   - 显示成功提示

**快速补货**（`/admin/inventory/stock_in/<part_id>`）：

1. **表单字段**（`StockInForm`）：
   - `add_quantity`：入库数量（必填，必须大于 0）
   - `new_price`：最新进货单价（可选，如果提供则更新配件单价）

2. **补货逻辑**：
   ```python
   part.current_stock += add_quantity
   if new_price is not None:
       part.price = new_price
   db.session.commit()
   ```
   - 直接增加库存数量
   - 如果提供了新价格，更新配件单价（反映最新进货成本）
   - 提交事务

**删除配件**（`/admin/inventory/delete/<part_id>`）：

1. **关联检查**：
   - 查询 `PartUsageDetail` 表，检查是否存在该配件的使用记录
   - 如果存在历史使用记录，禁止删除（保证数据完整性）

2. **删除逻辑**：
   - 如果无关联记录，执行 `db.session.delete(part)`
   - 提交事务
   - 显示成功提示

**低库存预警机制**：
- 在库存列表页面，系统自动检测 `current_stock < warning_line` 的配件
- 前端使用醒目的样式（如红色标识）提示管理员
- 仪表板统计低库存配件数量，供管理员快速了解补货需求

### 3.4.5 用户管理

#### 功能概述
管理员管理系统中所有用户（学生、维修工、管理员），包括新增、编辑、禁用用户。

#### 业务逻辑

**用户列表查询**：
- 支持分页显示
- 支持按角色筛选
- 支持按账号/姓名搜索

**新增用户**（`UserCreateForm`）：
- 表单字段：账号、姓名、角色、邮箱、所属宿舍楼、房间号
- 业务规则：
  - 学生角色必须绑定宿舍楼和填写房间号
  - 账号唯一性校验
  - 邮箱唯一性校验（如果提供）
  - 密码可选，默认密码为 "123456"

**编辑用户**（`UserEditForm`）：
- 账号不可修改（保证数据一致性）
- 可修改姓名、角色、邮箱、宿舍楼、房间号、账号状态
- 邮箱唯一性校验（排除当前用户）

**账号状态管理**：
- `status = 1`：正常（在校/在职）
- `status = 0`：禁用（离校/离职）
- 禁用用户无法登录系统

### 3.4.6 宿舍楼管理

#### 功能概述
管理员管理宿舍楼信息，包括新增、编辑、删除宿舍楼。

#### 业务逻辑

**宿舍楼列表**：
- 显示所有宿舍楼及其基本信息
- 显示每个宿舍楼关联的用户数和活跃工单数

**新增/编辑宿舍楼**（`BuildingForm`）：
- 表单字段：楼名、地理位置、宿管办公室电话
- 楼名唯一性校验

**删除宿舍楼**：
- 检查是否有关联用户或工单
- 如果存在关联数据，禁止删除或提示警告
- 如果无关联数据，执行删除操作

## 3.5 系统状态流转总览

### 3.5.1 工单状态流转图

```
PENDING (待处理)
    ↓ [管理员派单]
ASSIGNED (已派单)
    ↓ [维修工开始维修]
IN_PROGRESS (处理中)
    ↓ [所有维修工完工]
COMPLETED (已完成)
    ↓ [学生评价]
COMPLETED + rating (已完成并已评价)

PENDING (待处理)
    ↓ [学生取消]
CANCELLED (已取消)
```

### 3.5.2 状态流转触发条件

| 状态流转 | 触发操作 | 执行角色 | 关键逻辑 |
|---------|---------|---------|---------|
| `PENDING` → `ASSIGNED` | 管理员派单 | 管理员 | 更新 `order_assign` 中间表，设置 `assigned_workers` |
| `ASSIGNED` → `IN_PROGRESS` | 维修工开始维修 | 维修工 | 更新工单状态 |
| `IN_PROGRESS` → `COMPLETED` | 所有维修工完工 | 系统自动 | 统计 `MaintenanceRecord`，当完成数 >= 指派数时自动更新 |
| `PENDING` → `CANCELLED` | 学生取消工单 | 学生 | 仅允许取消待处理状态的工单 |

### 3.5.3 数据一致性保证

1. **事务处理**：
   - 派单操作：中间表更新与状态更新在同一事务中
   - 完工填报：维修记录创建、库存扣减、消耗明细创建在同一事务中
   - 工单完成判断：在事务提交前执行，确保状态更新的原子性

2. **关联数据完整性**：
   - 使用外键约束（`ondelete='RESTRICT'`）防止误删关联数据
   - 删除配件前检查历史使用记录
   - 删除宿舍楼前检查关联用户和工单

3. **库存扣减原子性**：
   - 库存校验与扣减在同一事务中执行
   - 如果库存不足，整个事务回滚，保证数据一致性
   - 支持并发场景下的库存扣减（通过数据库行锁保证）

